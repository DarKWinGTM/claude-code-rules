# v0.1.31 - Completed darkwingtm persistent install proof

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.31
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-19)

## Summary

Completed phase 026 for the selected target install ID `memory-context-intelligence@darkwingtm` in checked local scope.

The approved local install command succeeded from `<workspace-root>`, and normal plus bare Claude CLI checks show `memory-context-intelligence@darkwingtm` installed with `scope: "local"`, project path `<workspace-root>`, and cache path `<user-claude-root>/plugins/cache/darkwingtm/memory-context-intelligence/0.9.0`.

Phase 027 remains the first darkwingtm uninstall lifecycle closeout gate. No uninstall was performed in this version.

## Change details

- Marked `phase/phase-026-darkwingtm-persistent-install-proof.md` Completed.
- Recorded the exact install command:

  ```bash
  claude plugin install --scope local "memory-context-intelligence@darkwingtm"
  ```

- Recorded successful install output for local scope.
- Recorded normal CLI installed-plugin visibility from `claude plugin list --json`.
- Recorded bare CLI installed-plugin visibility from `claude --bare plugin list --json`.
- Recorded bare CLI details from `claude --bare plugin details "memory-context-intelligence@darkwingtm"`, including source identity, one skill, four agents, zero hooks, zero MCP servers, and always-on context.
- Recorded effective `darkwingtm` marketplace mapping from `claude plugin marketplace list --json`.
- Recorded checked local settings, user marketplace registry, installed-plugin registry, cache directory contents, cached manifest identity, and source-side marketplace evidence.
- Updated governed docs to describe phase 026 as completed and phase 027 as the first darkwingtm uninstall lifecycle closeout gate.
- Bumped version-owning capsule docs from `0.1.30` to `0.1.31` where applicable.

## Checked evidence

Phase 026 evidence includes:

```text
Installing plugin "memory-context-intelligence@darkwingtm"...✔ Successfully installed plugin: memory-context-intelligence@darkwingtm (scope: local)
```

Normal and bare plugin list evidence includes:

```text
id: memory-context-intelligence@darkwingtm
version: 0.9.0
scope: local
enabled: true
installPath: <user-claude-root>/plugins/cache/darkwingtm/memory-context-intelligence/0.9.0
projectPath: <workspace-root>
```

Bare details evidence includes:

```text
memory-context-intelligence 0.9.0
Source: memory-context-intelligence@darkwingtm
Skills (1)  memory-context-intelligence
Agents (4)  source-trust-reviewer, synthesis-lead, trace-scout, research-scout
Hooks (0)
MCP servers (0)
Always-on: ~400 tok added to every session
```

Checked persistent surfaces include:

- `<workspace-root>/.claude/settings.local.json`
- `<user-claude-root>/plugins/known_marketplaces.json`
- `<user-claude-root>/plugins/installed_plugins.json`
- `<user-claude-root>/plugins/cache/darkwingtm/memory-context-intelligence/0.9.0`
- `<user-claude-root>/plugins/cache/darkwingtm/memory-context-intelligence/0.9.0/.claude-plugin/plugin.json`
- `<repo-root>/plugin/.claude-plugin/marketplace.json`

## Observed side effect

Normal and bare plugin list output also show marketplace mismatch errors for existing installed `@darkwingtm` plugins that are not present in the current `<repo-root>/plugin` marketplace root.

This version records the side effect only. It does not repair existing installed `@darkwingtm` plugin state, shared marketplace normalization, or user-scope installed plugin state.

## Boundaries

This version does not claim:

- uninstall lifecycle behavior for `memory-context-intelligence@darkwingtm`
- slash-command/chat invocation behavior
- publication or external marketplace release
- stable or broad production readiness
- main RULES promotion, mutation, or merge
- `/additional/` behavior change
- repair of existing installed `@darkwingtm` plugin marketplace mismatch errors

## Verification

Verification route: `smoke_check`.

Ran in checked local scope:

```bash
claude plugin install --scope local "memory-context-intelligence@darkwingtm"
claude plugin list --json
claude --bare plugin list --json
claude --bare plugin details "memory-context-intelligence@darkwingtm"
claude plugin marketplace list --json
```

Result: passed for local-scope persistent install visibility and details of `memory-context-intelligence@darkwingtm`.
