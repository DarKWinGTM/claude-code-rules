# v0.1.61 - Completed analysis skill wrapper permission-safe correction

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.61
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-28)

---

## Summary

Completed phase 057 as the runtime-facing correction that replaced the permission-blocked inline analysis skill wrapper with a fixed-command runtime subcommand and restored transcript-visible local slash proof for `/memory-context-intelligence:analysis`.

## Changes

- replaced the inline heredoc / shell-expansion executable block in `skills/analysis/SKILL.md` with the fixed-command `memory-context-intelligence analysis-surface` entrypoint
- added `lib/analysis_surface.py` as the runtime owner for the former inline wrapper logic
- added the internal `analysis-surface` subcommand to `bin/memory-context-intelligence`
- updated the active contract wording from unresolved empty print-mode proof to approved non-interactive local slash proof
- bumped plugin package version from `0.9.13` to `0.9.14`

## Verification

- focused analysis-skill contract tests passed
- `python3 -m py_compile lib/analysis_surface.py` passed
- full runtime/source suite passed with `63` tests green
- source-session slash proof via `--plugin-dir` plus `--permission-mode bypassPermissions` returned operator-facing output
- installed-local slash proof via `--permission-mode bypassPermissions` returned operator-facing output after updating the local package to `0.9.14`

## Boundary

This version does not change the phase 056 breadth/promotion semantics, does not weaken the trace-anchored evidence model into context-only promotion, does not change `/additional/`, and does not claim plugin-managed auto-flow proof, publication, external marketplace release, stable/broad readiness, or main RULES promotion/mutation/merge.
