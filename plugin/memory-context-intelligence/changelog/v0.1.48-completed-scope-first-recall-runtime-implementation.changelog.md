# v0.1.48 - Completed scope-first recall runtime implementation

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.48
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-20)

---

## Summary

Completed phase 044 as the runtime implementation wave for the phase-043 scope-first recall contract.

## Changes

- Implemented explicit current-day shard selection, current-session scoping, optional same-day lookback, and same-day widening after current-session insufficiency in `lib/intake.py`.
- Kept older replay/trial/readiness fixture coverage working by restoring compatibility for legacy bullet-only daily shard content alongside the newer timed shard format with session anchors.
- Updated the runtime `analysis` skill contract so the current day, current session, lookback, and same-day widening behavior are explicit in the active surface wording.
- Corrected readiness phase-007 package checks so the current runtime skill path is `skills/analysis/SKILL.md` rather than the retired self-named skill path.
- Verified `tests/test_intake.py`, `tests/test_analysis_skill_contract.py`, and the full package suite with `34` passing tests.
- Verified the packaged runtime `intake → signals → present` chain against current-day/current-session input, including a `120` minute lookback run, without relying on raw `session_id` semantic search alone.
- Recorded the checked current limitation that `claude -p` print-mode invocations of `/analysis` and `/memory-context-intelligence:analysis` returned empty zero-turn success results, so that print-mode path is not used as output proof in this wave.

## Boundary

This version does not claim plugin-managed auto-flow proof, publication, external marketplace release, stable behavior, broad production readiness, main RULES promotion, main RULES mutation, main RULES merge, new public `review`/`packet` surfaces, or `/additional/` behavior change.
