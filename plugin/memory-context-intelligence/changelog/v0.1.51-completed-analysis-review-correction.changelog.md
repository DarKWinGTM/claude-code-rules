# v0.1.51 - Completed analysis review correction

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.51
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-20)

---

## Summary

Completed phase 047 as the design-grounded current-session-first correction wave for `/memory-context-intelligence:analysis`.

## Changes

- Corrected the active contract so the first-pass analysis scope stays current-session-first inside the current day shard.
- Changed same-day widening from a default follow-on path to an explicit fallback-only path such as `fallback=same-day` after current-session insufficiency.
- Reframed first-response topics as design-grounded RULES/workflow review topics instead of generic recurring-pattern summaries.
- Kept provenance visible in first-response output so the user can tell whether a topic came from current-session-only evidence or from an explicitly selected same-day fallback path.
- Synced source-authority and runtime-facing docs to the corrected review behavior while preserving the checked plugin-owned surface `/memory-context-intelligence:analysis`.

## Boundary

This version preserves the existing scope-first recall engine, keeps `review` and `packet` deferred, keeps `/additional/` unchanged, and does not claim plugin-managed auto-flow proof, publication, external marketplace release, stable/broad production readiness, or main RULES promotion/mutation/merge.
