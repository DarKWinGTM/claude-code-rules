# v0.1.76 - Completed split-contract release closeout

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.76
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-06-02)

---

## Summary

Closed the plugin-scoped release wave for the per-topic additional-artifact split contract: preserved one selected topic per artifact plus combined-output-forbidden behavior, kept `/memory-context-intelligence:analysis` as the checked public surface, bumped the package version to `0.9.28`, and aligned the active governed chain to `0.1.76` in checked local scope.

## Changes

- kept the packet/additional split-contract runtime guard and focused test coverage from `v0.1.75` intact in the released state
- bumped `.claude-plugin/plugin.json`, skill frontmatter, README package-version wording, and manifest test expectation from `0.9.27` to `0.9.28`
- synced the active governed version state from `0.1.75` to `0.1.76`
- added phase-072 release-closeout tracking so the plugin-only release boundary stays explicit after the version bump
- preserved `trace_evidence` as the live promotion anchor, deferred `review` / `packet`, and kept the public surface at `/memory-context-intelligence:analysis`

## Verification

- the same focused split-contract/runtime/docs checks from phase 071 must still pass after the release-version bump
- the full plugin suite must pass at `0.9.28` / `0.1.76` state
- released docs must still say that any future multi-topic carry-forward must split into separate per-topic artifacts rather than one combined file

## Boundary

This version does not add a new public surface, does not reopen `review` or `packet`, does not weaken `trace_evidence` as the live promotion anchor, does not mutate main RULES, and does not claim broad production readiness beyond checked scope.
