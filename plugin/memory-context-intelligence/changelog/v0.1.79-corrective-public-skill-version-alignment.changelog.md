# Corrective public-skill version alignment

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.79
> **Session:** 48a3ef9b-50cf-4574-8f00-4f6b6e28f76e (2026-08-09)
> **Status:** Clean corrective package 0.9.31 candidate verified; external-action confirmation pending

---

## What changed

A post-release audit found that the immutable package `0.9.30` tag retained `skills/init/SKILL.md` at `0.9.29` while the manifest and analysis skill were `0.9.30`. The audit also found active wording drift in the analysis config-path explanation, the evidence-source design boundary, and the v0.1.78 planning/history surfaces.

The corrective implementation:
- aligns the manifest plus both public skills at package `0.9.31`
- adds a regression test that requires both skill versions to equal the manifest version
- describes `suggested_config_path` as the user-scope default config file
- restores `design/04-native-agent-orchestration.design.md` to its tag-backed last-change version `0.1.75`
- updates the selected evidence-source design shard to v0.1.79 and records the verified source/test/smoke/tagged-release boundary
- converts stale planning/current-state wording into accurate historical or delivered wording
- preserves the published `memory-context-intelligence--v0.9.30` tag and GitHub Release without movement or deletion

## Verification status

Local focused version/skill checks pass (`21 passed`), the full source suite passes (`132 passed`), and source package validation passes. A clean v0.9.30-based candidate also passes the full suite (`132 passed`) and package validation with exactly 17 allowlisted plugin paths and zero deletions. Explicit external-action confirmation plus remote branch/tag/GitHub Release/fresh-tag proof remain required before package `0.9.31` can be called released.

## Boundaries

- `0.9.30` remains immutable historical release evidence.
- No Main RULES mutation, promotion, or merge is selected.
- Marketplace installation and stability over time remain outside scope.
- No external `0.9.31` action is authorized until a new exact candidate SHA and scope receive explicit confirmation.
