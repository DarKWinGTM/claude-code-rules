# v0.1.64 - Completed clean presentation spine and related variants

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.64
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-30)

---

## Summary

Completed phase 060 as the first-response cleanup wave that keeps one clean `presentation / recommendation / proposal / related variants` spine, expands one primary proposal, and compresses weaker same-family follow-ups into `related variants`.

## Changes

- updated `lib/presentation.py` so the main `topic_list` / `presentation.topic_list` now keeps only primary topic families once weaker same-family variants exist
- added an explicit `first_response_contract` in `lib/presentation.py` so section order, section roles, and the no-repeated-recap policy are visible in the structured output
- kept weaker same-family candidates visible under `related_variants` instead of repeating them as separate main-list entries
- updated `lib/analysis_surface.py` so the checked analysis context now forwards `related_variants`, `first_response_spine`, and `first_response_contract`
- updated `skills/analysis/SKILL.md` and governed docs so the public contract now states `presentation / recommendation / proposal / related variants` explicitly, requires one section per role, and forbids repeating same-family weaker topics or repeated recap blocks after `presentation`
- bumped the source package version from `0.9.18` to `0.9.19`

## Verification

- focused `tests/test_presentation.py` passed after a failing RED check for the missing explicit section-role / no-repeat contract
- focused `tests/test_analysis_surface.py` passed after a failing RED check for the missing forwarded `related_variants` payload and explicit first-response contract fields
- focused `tests/test_analysis_skill_contract.py` passed after a failing RED check for missing one-section-per-role / no-repeated-recap wording
- full runtime/source suite passed with `78` tests
- checked source-authority `/memory-context-intelligence:analysis` run via `--plugin-dir` returned a primary-family-only main list plus an explicit `Related variants` section in real slash output

## Boundary

This version does not collapse doctrine-level titles back into incident-level wording, does not weaken the trace-anchored evidence model into context-only promotion, does not invent examples when usable bounded preview evidence is absent, does not emit raw memory dumps, does not change `/additional/`, and does not claim plugin-managed auto-flow proof, publication, external marketplace release, stable/broad readiness, or main RULES promotion/mutation/merge.
