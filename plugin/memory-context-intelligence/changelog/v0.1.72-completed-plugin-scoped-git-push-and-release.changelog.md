# v0.1.72 - Completed plugin-scoped git push and release

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.72
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-06-01)

---

## Summary

Closed a plugin-scoped release wave for `memory-context-intelligence` from the RULES repo: the capsule is now tracked directly in RULES git history, the package version is bumped to `0.9.25`, plugin-scoped release metadata is synced, and the release path is isolated from unrelated RULES repo changes.

## Changes

- tracked `plugin/memory-context-intelligence/**` directly inside the RULES repo index/history instead of leaving the capsule as an untracked source-only tree
- bumped `.claude-plugin/plugin.json` and the skill-frontmatter package version from `0.9.24` to `0.9.25`
- synced the active governed version surfaces from `0.1.71` to `0.1.72` for the plugin-local design/changelog/phase/README chain
- added a dedicated phase-068 release record plus a plugin-scoped release patch artifact for this wave
- kept `review` / `packet` deferred and left phase-067 config-policy boundaries unchanged

## Verification

- `git status --short` and `git diff --cached --name-only` are used to confirm plugin-scoped staging and isolation from unrelated repo changes before commit
- the full plugin test suite must pass at `0.9.25` / `0.1.72` state before push/release
- the branch push, commit hash, plugin-specific tag, and release output are verified in transcript during execution
- release notes come from this plugin-local shard so the outward-facing release surface references `memory-context-intelligence` directly

## Boundary

This version does not claim new runtime semantics beyond the already-checked phase-067 config-policy behavior. It only closes the repo-tracking / push / release wave for the existing plugin scope, keeps unrelated RULES repo changes out of the plugin-scoped commit, and does not reopen `review`, `packet`, `/additional/`, publication-beyond-plugin, or broad production-readiness claims.
