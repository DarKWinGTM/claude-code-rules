# Changelog Detail - v0.1.25 completed session-only load proof

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.25
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-18)

---

## Summary

Completed phase 021 as the session-only inline load-proof gate for `memory-context-intelligence`.

This version records transcript-visible Claude Code CLI evidence that the plugin can be loaded from the checked source package for one session using `--plugin-dir`. It does not claim persistent install proof, reload proof, uninstall proof, slash-command/chat invocation proof, publication, external marketplace release, main RULES promotion, main RULES mutation, or main RULES merge.

## Changes

- bumped capsule governed docs to version `0.1.25`
- updated `phase/phase-021-session-only-load-proof.md` to Completed
- recorded the exact phase-021 proof commands and observed output shape
- updated `phase/SUMMARY.md` to mark phase 021 completed and phase 022 as the first persistent-install proof gate
- updated `design/06-plugin-installability.design.md` and `design/design.md` to preserve session-only `@inline` proof boundaries
- updated `README.md`, the active patch, and root TODO to reflect session-only load proof only
- added this `0.1.25` version detail shard and indexed it from the active parent changelog

## Proof commands

```bash
claude --bare --plugin-dir "<repo-root>/plugin/memory-context-intelligence" plugin details memory-context-intelligence
claude --bare --plugin-dir "<repo-root>/plugin/memory-context-intelligence" plugin list --json
```

## Relevant observed output

`plugin details` showed:

```text
memory-context-intelligence 0.9.0
  Source: memory-context-intelligence@inline

Component inventory
  Skills (1)  memory-context-intelligence
  Agents (4)  source-trust-reviewer, synthesis-lead, trace-scout, research-scout
```

`plugin list --json` showed the phase-021 target plugin entry as:

```json
{
  "id": "memory-context-intelligence@inline",
  "version": "0.9.0",
  "scope": "session",
  "enabled": true,
  "installPath": "<repo-root>/plugin/memory-context-intelligence"
}
```

## Boundaries preserved

- active runtime source home remains `<repo-root>/plugin/memory-context-intelligence/`
- phase 021 proof uses session-scoped inline loading through `--plugin-dir`
- `/additional/` trial-stage behavior remains unchanged
- phase 022 is the first persistent-install proof gate and remains approval-gated before any persistent runtime/settings mutation
- no persistent Claude runtime install state, external install/cache location, or `settings.json` was intentionally changed by this phase
- persistent install proof, reload proof, uninstall proof, slash-command/chat invocation proof, publication proof, marketplace release proof, stable/broad production readiness, main RULES promotion, main RULES mutation, and main RULES merge remain unclaimed

## Verification summary

Session-only inline load proof completed in checked scope. The checked CLI evidence shows `memory-context-intelligence@inline` loaded with `scope: "session"` from `<repo-root>/plugin/memory-context-intelligence`, with the `memory-context-intelligence` skill and four package agents visible in that command/session context.

No persistent install, reload/new-session persistence, uninstall, external marketplace publication, slash-command/chat invocation, or main RULES promotion behavior is verified by this version.
