# Changelog Detail - v0.1.27 completed install lifecycle closeout

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.27
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-19)

---

## Summary

Completed phase 023 as the checked local-scope install lifecycle closeout for `memory-context-intelligence`.

This version records that phase 023 reused the phase-022 normal plus bare CLI checks as the reload/new-process evidence side, then completed the approved uninstall-only proof for `memory-context-intelligence@rules-local`. It closes installability family 019-023 in checked local scope only. It does not claim slash-command/chat invocation proof, publication, external marketplace release, stable/broad production readiness, main RULES promotion, main RULES mutation, or main RULES merge.

## Changes

- bumped capsule governed docs to version `0.1.27`
- marked phase 023 Completed
- closed installability family 019-023 in checked local scope
- reused phase-022 normal and bare CLI list/details evidence as the reload/new-process side for `memory-context-intelligence@rules-local`
- completed the approved uninstall-only command with `--keep-data`
- recorded that normal and bare installed-plugin lists no longer include `memory-context-intelligence@rules-local`
- recorded that bare plugin details for `memory-context-intelligence@rules-local` fails with not found after uninstall
- recorded that local `enabledPlugins` is cleared and no longer contains `memory-context-intelligence`
- recorded that the `rules-local` marketplace remains intentionally registered and still points at `<repo-root>/plugin`
- recorded that the source package and `/additional/` trial-stage material are preserved
- recorded that retained cache/data remains present after `--keep-data` and must not be described as an installed/enabled plugin entry
- updated README, design, phase, skill, changelog, patch, and root TODO wording to distinguish checked local install lifecycle closeout from publication, external marketplace release, slash-command/chat invocation proof, stable/broad production readiness, or main RULES promotion/merge proof

## Proof commands

Reload/new-process side reused from phase 022, run from `<workspace-root>` without `--plugin-dir`:

```bash
claude plugin list --json
claude --bare plugin list --json
claude --bare plugin details "memory-context-intelligence@rules-local"
```

Approved uninstall-only command, run from `<workspace-root>`:

```bash
claude plugin uninstall --scope local --keep-data "memory-context-intelligence@rules-local"
```

Post-uninstall checks:

```bash
claude plugin marketplace list --json
claude plugin list --json
claude --bare plugin list --json
claude --bare plugin details "memory-context-intelligence@rules-local"
```

## Relevant observed output

The phase-022 normal and bare CLI checks showed `memory-context-intelligence@rules-local` with:

```json
{
  "id": "memory-context-intelligence@rules-local",
  "version": "0.9.0",
  "scope": "local",
  "enabled": true,
  "installPath": "<user-claude-root>/plugins/cache/rules-local/memory-context-intelligence/0.9.0",
  "projectPath": "<workspace-root>"
}
```

The phase-022 bare details check showed:

```text
memory-context-intelligence 0.9.0
  Source: memory-context-intelligence@rules-local

Component inventory
  Skills (1)  memory-context-intelligence
  Agents (4)  source-trust-reviewer, synthesis-lead, trace-scout, research-scout
```

The phase-023 uninstall command reported:

```text
✔ Successfully uninstalled plugin: memory-context-intelligence (scope: local)
```

`claude plugin marketplace list --json` still showed `rules-local` as a directory marketplace with path `<repo-root>/plugin` and install location `<repo-root>/plugin`.

`claude plugin list --json` and `claude --bare plugin list --json` no longer included `memory-context-intelligence@rules-local`.

`claude --bare plugin details "memory-context-intelligence@rules-local"` failed with:

```text
Plugin "memory-context-intelligence@rules-local" not found. Run `claude plugin list` to see installed plugins, or pass --plugin-dir <path> to load one from disk.
```

## Runtime/settings surfaces observed

- `<workspace-root>/.claude/settings.local.json` now has `enabledPlugins: {}`, retains `extraKnownMarketplaces.rules-local.source.path = <repo-root>/plugin`, and no longer contains `memory-context-intelligence`.
- `<workspace-root>/.claude/plugins` remains absent.
- `<user-claude-root>/plugins/installed_plugins.json` no longer contains `memory-context-intelligence`.
- `<user-claude-root>/plugins/cache/rules-local/memory-context-intelligence` remains present after `--keep-data`, with child `0.9.0`; docs treat this as retained cache/data, not an installed/enabled plugin entry.
- `<repo-root>/plugin/memory-context-intelligence/` remains preserved as the source package.
- `<user-runtime-rules>/additional/memory-context-intelligence/phase-015-live-bounded-additional-stage-trial.md` remains preserved as trial-stage material.

## Boundaries preserved

- active runtime source home remains `<repo-root>/plugin/memory-context-intelligence/`
- `rules-local` marketplace registration remains intentionally retained
- `memory-context-intelligence@inline` remains the phase-021 session-only load proof ID only
- `memory-context-intelligence@rules-local` remains the phase-022 local-scope persistent install proof ID and phase-023 uninstall target
- `/additional/` trial-stage behavior remains unchanged
- retained cache/data is not an installed/enabled plugin-list entry
- local settings/cache outputs are install lifecycle evidence, not a new active implementation home
- slash-command/chat invocation proof, publication proof, external marketplace release proof, stable/broad production readiness, main RULES promotion, main RULES mutation, and main RULES merge remain unclaimed

## Verification summary

The checked local installability lifecycle is complete through phase 023. The package has checked-scope usability through phase 016, source-side bootstrap readiness through phase 020, session-only inline load proof through phase 021, local-scope persistent CLI install proof through phase 022, and approved uninstall-only lifecycle closeout through phase 023.

No publication, external marketplace release, slash-command/chat invocation, stable/broad production readiness, main RULES promotion, main RULES mutation, or main RULES merge behavior is verified by this version.
