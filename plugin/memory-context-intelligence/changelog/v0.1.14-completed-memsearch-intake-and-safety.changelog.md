# Version 0.1.14 - Completed memsearch intake and safety

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.14
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-18)

---

## Changes

- completed phase 008 by adding bounded safe memsearch intake to the isolated runtime package under `<repo-root>/plugin/memory-context-intelligence/`
- extended the package command surface with an `intake` subcommand in addition to help/status/no-op behavior
- added a no-dependency intake helper that checks explicit memory-root availability, discovers date-named daily markdown shards, reads a bounded scoped subset, classifies available/stale/insufficient/unavailable states, minimizes sensitive sample content, and emits an evidence-strength-labeled JSON report
- preserved late-bound configuration by requiring `--memory-root`, `MEMORY_CONTEXT_INTELLIGENCE_MEMORY_ROOT`, or `MCI_MEMORY_ROOT`; no machine-specific memsearch root is a package default
- synced package README, design, changelog, phase, patch, skill, and trace-scout surfaces to the phase-008 intake boundary
- updated capsule README, design parent, phase 008, phase summary, patch, and root TODO wording to show phase 008 complete and phases 009+ still pending

## Verification

- ran syntax compilation by compiling `lib/intake.py` source in memory without writing package bytecode caches
- ran `bash "<repo-root>/plugin/memory-context-intelligence/bin/memory-context-intelligence" --help`
- ran intake against the checked local memsearch root `<workspace-root>/.memsearch/memory` with a scoped bounded sample
- ran intake against `/tmp/mci-phase008-missing-memory-root` to verify unavailable handling
- ran intake against the checked local memsearch root with an unmatched scope to verify insufficient handling
- ran intake against the checked local stale memsearch root `<template-plugin-root>/media-generator/.memsearch/memory` with a one-day freshness threshold to verify stale handling
- checked that `<user-runtime-rules>/additional` was not created during phase 008 verification

## Summary

This version closes phase 008 by making memory intake real but bounded. The runtime can now consume memsearch daily markdown shards as observational work-trace input and gracefully report unavailable, stale, or insufficient input. Signal extraction, topic generation, external research, native-agent orchestration, candidate packet creation, `/additional/` emission, runtime usability release, and main RULES promotion remain gated by later phases.
