# Completed init configuration surface

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.77
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-06-02)

---

## What changed

This wave adds `/memory-context-intelligence:init` as the public setup/config surface, extends the config contract with `analysis.scope_policy`, moves the default config target to `~/.claude/memory-context-intelligence.config.json`, and makes the analysis runtime consume stored default scope only when the operator did not explicitly narrow the run.

## Boundaries kept

- `/memory-context-intelligence:analysis` remains the review surface
- `trace_evidence` remains the live promotion anchor
- `review` and `packet` remain deferred/non-public
- no main RULES mutation or publication claims were added in this wave
