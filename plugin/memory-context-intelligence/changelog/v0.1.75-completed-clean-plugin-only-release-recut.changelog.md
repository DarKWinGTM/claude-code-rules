# v0.1.75 - Completed clean plugin-only release recut

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.75
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-06-01)

---

## Summary

Re-cut the `memory-context-intelligence` plugin release from an isolated clean checkout so the branch-first merge, push, tag, and GitHub release path are now proven without unrelated RULES-root or `plugin/governed-docs` working-tree drift, while preserving the same plugin-specific README cleanup and adaptive deep-analysis runtime contract.

## Changes

- preserved the phase-070 plugin payload semantics while bumping the plugin package version from `0.9.26` to `0.9.27`
- synced the active governed version state from `0.1.74` to `0.1.75`
- kept the plugin README free of the Claude Code installation tutorial, shell-prerequisite setup steps, and `/login` bootstrap guidance
- added a dedicated phase-071 closeout record plus a clean-release patch artifact for this corrective wave
- re-published the plugin from an isolated clean checkout so the pushed branch/tag/release path is proven at plugin-only scope

## Verification

- the release checkout must stay clean before merge/push so unrelated RULES-root and `plugin/governed-docs` drift is outside the release workspace
- the commit diff/stat must stay limited to `plugin/memory-context-intelligence/**`
- the README diff must continue to show the Claude Code installation tutorial removed without losing plugin-specific install/use guidance
- the full plugin suite must pass at `0.9.27` / `0.1.75` state before push/release
- plugin manifest validation, merge-to-`master`, push to `origin/master`, tag creation, and GitHub release must all succeed for the corrective version

## Boundary

This version does not change the public runtime contract beyond the already-checked adaptive deep-analysis behavior. `/memory-context-intelligence:analysis` remains the only proved public surface, `review` and `packet` remain deferred, `trace_evidence` remains the live promotion anchor, and this release still does not claim broad production readiness or main RULES promotion/mutation.
