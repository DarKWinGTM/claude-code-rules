# Changelog Detail - v0.1.26 completed persistent install proof

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.26
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-18)

---

## Summary

Completed phase 022 as the checked local-scope persistent CLI install-proof gate for `memory-context-intelligence`.

This version records transcript-visible Claude Code CLI evidence that the plugin can be installed from the source-side `rules-local` marketplace and remains visible in `<workspace-root>` without `--plugin-dir`. It does not claim reload proof, uninstall proof, rollback proof, slash-command/chat invocation proof, publication, external marketplace release, stable/broad production readiness, main RULES promotion, main RULES mutation, or main RULES merge.

## Changes

- bumped capsule governed docs to version `0.1.26`
- renamed the source-side local marketplace from `darkwingtm` to `rules-local` to avoid colliding with the existing local `darkwingtm` marketplace path
- kept package author metadata as lowercase `darkwingtm` with URL `https://github.com/darkwingtm`
- kept the marketplace-local plugin entry as `memory-context-intelligence` sourced from `./memory-context-intelligence`
- installed the plugin in local scope as `memory-context-intelligence@rules-local`
- recorded normal and bare CLI list/details evidence without `--plugin-dir`
- recorded local settings/cache surfaces observed after the install
- updated phase 022 to Completed and kept phase 023 as the first reload/uninstall/rollback closeout gate
- updated README, design, phase, patch, and root TODO wording to distinguish local persistent CLI install proof from reload, uninstall, publication, external release, slash-command/chat invocation, stable/broad production readiness, or main RULES promotion/merge proof

## Proof commands

Run from `<workspace-root>`:

```bash
claude plugin marketplace add --scope local "<repo-root>/plugin"
claude plugin install --scope local "memory-context-intelligence@rules-local"
claude plugin marketplace list --json
claude plugin list --json
claude --bare plugin list --json
claude --bare plugin details "memory-context-intelligence@rules-local"
```

## Relevant observed output

The marketplace add command reported:

```text
Successfully added marketplace: rules-local (declared in local settings)
```

The install command reported:

```text
Successfully installed plugin: memory-context-intelligence@rules-local (scope: local)
```

`claude plugin marketplace list --json` showed `rules-local` as a directory marketplace pointing at `<repo-root>/plugin`, while the existing `darkwingtm` marketplace still pointed at `<template-plugin-root>`.

`claude plugin list --json` and `claude --bare plugin list --json` showed the phase-022 target plugin entry as:

```json
{
  "id": "memory-context-intelligence@rules-local",
  "version": "0.9.0",
  "scope": "local",
  "enabled": true,
  "installPath": "<user-claude-root>/plugins/cache/rules-local/memory-context-intelligence/0.9.0",
  "projectPath": "<workspace-root>"
}
```

`claude --bare plugin details "memory-context-intelligence@rules-local"` showed:

```text
memory-context-intelligence 0.9.0
  Source: memory-context-intelligence@rules-local

Component inventory
  Skills (1)  memory-context-intelligence
  Agents (4)  source-trust-reviewer, synthesis-lead, trace-scout, research-scout
```

## Runtime/settings surfaces observed

- `<workspace-root>/.claude/settings.local.json` changed in checked scope and now contains `enabledPlugins.memory-context-intelligence@rules-local = true` plus `extraKnownMarketplaces.rules-local.source.path = <repo-root>/plugin`.
- `<workspace-root>/.claude/settings.json` remained unchanged in the checked snapshot.
- `<user-claude-root>/settings.json` remained unchanged in the checked snapshot.
- `<user-claude-root>/plugins/cache/rules-local/memory-context-intelligence/0.9.0` exists as the Claude plugin cache copy for the local-scope install; a post-install snapshot found `102` files there.
- `<user-claude-root>/plugins/marketplaces/rules-local` was not created; the marketplace list reports `rules-local` as a directory source whose install location is `<repo-root>/plugin`.
- `<workspace-root>/.claude/plugins` remained missing.

## Boundaries preserved

- active runtime source home remains `<repo-root>/plugin/memory-context-intelligence/`
- the marketplace-qualified persistent install ID is `memory-context-intelligence@rules-local`, not `memory-context-intelligence@darkwingtm` or `memory-context-intelligence@inline`
- `memory-context-intelligence@inline` remains the phase-021 session-only load proof ID only
- `/additional/` trial-stage behavior remains unchanged
- local settings/cache outputs from phase 022 are install evidence, not a new active implementation home
- phase 023 remains required for reload, uninstall, rollback, and install lifecycle closeout
- slash-command/chat invocation proof, publication proof, external marketplace release proof, stable/broad production readiness, main RULES promotion, main RULES mutation, and main RULES merge remain unclaimed

## Verification summary

Local-scope persistent CLI install proof completed in checked scope. The checked CLI evidence shows `memory-context-intelligence@rules-local` installed with `scope: "local"` for project path `<workspace-root>`, using cache install path `<user-claude-root>/plugins/cache/rules-local/memory-context-intelligence/0.9.0`, with the `memory-context-intelligence` skill and four package agents visible through normal and bare CLI details without `--plugin-dir`.

No reload/new-session persistence, uninstall, rollback, external marketplace publication, slash-command/chat invocation, stable/broad production readiness, or main RULES promotion/merge behavior is verified by this version.
