# v0.1.47 - Completed recall-scoped analysis design clarification

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.47
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-20)

---

## Summary

Completed phase 043 as the docs/design clarification wave for how `/analysis` should use memsearch recall without defaulting to broad all-history input.

## Changes

- Read checked local memsearch docs/runtime surfaces and recorded the current recall model as search → expand → transcript drill-down.
- Documented that checked public recall behavior currently proves query/top-k/source-prefix scoping, but not first-class `session/day/lookback` flags.
- Recorded checked local evidence that exact `session_id` search within a same-day `source_prefix` still returns mixed-session results, so raw UUID search is not the deterministic selector for `/analysis`.
- Added the selected `/analysis` design contract: current project → current day → current session by default, with optional same-day lookback and same-day-only widening before any broader history.
- Kept implementation explicitly deferred until the recall-scoped contract is selected and clear.

## Boundary

This version is a docs-only design clarification wave. It does not mutate runtime behavior, `/additional/`, public command surfaces beyond active design wording, plugin-managed auto-flow proof, publication, external marketplace release, stable/broad production readiness, or main RULES promotion/mutation/merge.
