# Version 0.1.10 - Normalized plugin-local design and changelog chains

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.10
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-18)

---

## Changes
- switched the local `design/` chain to a generic active parent because the current `design/` folder fully scopes one chain
- kept topic-level design documents as flat sibling shards under `design/`
- switched the local `changelog/` chain to a generic active parent because the current `changelog/` folder fully scopes one chain
- kept version-detail changelog files as flat sibling shards under `changelog/`
- demoted semantic parent filenames to compatibility-only notes instead of active owners
- updated README, phase, patch, design, and changelog references to the new active parent model

## Summary
This version keeps the user-selected plugin-local governed structure while aligning the active parent model to the current RULES doctrine for a fully scoped local folder. The chain now uses `design/design.md` and `changelog/changelog.md` as active parents with flat sibling shards beside them.
