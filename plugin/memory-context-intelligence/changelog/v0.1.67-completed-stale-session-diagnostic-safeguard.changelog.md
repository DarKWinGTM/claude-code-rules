# v0.1.67 - Completed stale-session diagnostic safeguard

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.67
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-30)

---

## Summary

Completed phase 063 as the temporary stale-session diagnostic safeguard wave that makes long-lived session freshness mismatch visible in checked scope without changing the normal `/memory-context-intelligence:analysis` status flow and without normalizing the underlying session-dependent bug.

## Changes

- updated `lib/analysis_surface.py` to compare the current session freshness evidence against installed plugin metadata and append one advisory `stale_long_lived_session` warning only when checked evidence supports it
- repaired the source `.claude-plugin/plugin.json` syntax regression so the plugin manifest is valid JSON again and fresh plugin registration no longer drops the slash command from new Claude sessions
- kept the warning additive only so `available`, `blocked`, `dormant`, `no-topics`, repeated topic cards, and `Next action options` remain the normal operator-facing output contract
- updated `tests/test_analysis_surface.py` to cover positive warning emission, missing-evidence no-warning behavior, and non-normalization of status/topic output
- added `tests/test_plugin_manifest.py` so manifest JSON validity and the current package version are checked directly
- updated `skills/analysis/SKILL.md` and governed docs so restart guidance stays temporary/diagnostic only and session-dependent no-response remains a bug rather than acceptable normal behavior
- bumped the source package version from `0.9.21` to `0.9.22`

## Verification

- focused `tests/test_plugin_manifest.py` passed after fixing the malformed source manifest
- focused `tests/test_analysis_surface.py` passed with the new stale-session safeguard coverage
- focused `tests/test_analysis_skill_contract.py` passed after adding the non-normalizing warning contract wording
- focused `tests/test_presentation.py`, `tests/test_analysis_surface.py`, and `tests/test_analysis_skill_contract.py` passed with `35` checks
- full runtime/source suite passed with `86` tests
- fresh installed-local `claude -p '/memory-context-intelligence:analysis' --permission-mode bypassPermissions --output-format json` now registers the plugin again and returns normal topic-card output instead of `Unknown command`
- targeted installed-cache `analysis-surface --session-id d42465eb-30a7-4bc8-b9d6-03e52306e9a5` proof now emits `operator_warnings[0].type = stale_long_lived_session` while preserving `status = available`, repeated `topic_cards`, and `next_action_options`

## Boundary

This version adds a temporary operator safeguard only. It does not prove the exact internal stale long-lived-session slot, does not make restart the final fix, does not normalize session-dependent no-response as acceptable behavior, does not change `/additional/`, and does not claim plugin-managed auto-flow proof, publication, external marketplace release, stable/broad readiness, or main RULES promotion/mutation/merge.
