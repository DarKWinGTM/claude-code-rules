# v0.1.49 - Completed registered analysis surface correction

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.49
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-20)

---

## Summary

Completed phase 045 as the docs/test correction wave for the checked registered analysis slash surface.

## Changes

- Rechecked the current local slash registry and recorded that it includes `memory-context-intelligence:analysis` but not bare `analysis`.
- Rechecked `claude -p --output-format json` behavior and recorded that both `/analysis` and `/memory-context-intelligence:analysis` returned zero-turn empty success results in print mode, so that path is not used as execution proof.
- Corrected active source/runtime docs so they no longer present bare `/analysis` as the proved current runtime surface.
- Preserved the packaged scope-first runtime proof from focused tests, the full package suite, and direct packaged `intake → signals → present` execution.
- Updated the runtime analysis-skill contract tests to the corrected registered-surface wording.

## Boundary

This version is a governance/test correction wave only. It does not change the scope-first runtime implementation, `/additional/` behavior, plugin-managed auto-flow proof, publication, external marketplace release, stable behavior, broad production readiness, or main RULES promotion/mutation/merge.
