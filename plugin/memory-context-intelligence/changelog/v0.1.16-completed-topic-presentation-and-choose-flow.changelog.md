# Version 0.1.16 - Completed topic presentation and choose flow

> **Parent Document:** [changelog.md](changelog.md)
> **Current Version:** 0.1.16
> **Session:** d42465eb-30a7-4bc8-b9d6-03e52306e9a5 (2026-05-18)

---

## Changes

- completed phase 010 by adding user-facing topic presentation and choose-flow behavior to the isolated runtime package under `<repo-root>/plugin/memory-context-intelligence/`
- extended the package command surface with `present` and `choose` subcommands that consume phase-009 signals JSON rather than rereading memory sources
- added `lib/presentation.py` as a no-dependency helper for list-first topic rendering, output-mode handling, canonical identifier preservation, and fileless selected-topic state
- added support for `auto`, `native-first`, `bilingual`, and `fixed` presentation modes while preserving topic ids, file paths, commands, config keys, env vars, and evidence anchors exactly as carried by the signals report
- added choose-flow output that records exactly one selected topic as carry-forward eligible and keeps every unselected topic advisory-only
- added focused tests under `tests/test_presentation.py` for output modes, canonical identifier preservation, selection-required carry-forward, and advisory-only unselected topics
- synced package README, design, changelog, phase summary, patch, skill, and plugin metadata to the phase-010 presentation boundary
- updated capsule README, design parent, topic-list design shard, phase 010, phase summary, patch, changelog, and root TODO wording to show phase 010 complete and phases 011+ still pending

## Verification

- ran syntax compilation by compiling `lib/intake.py`, `lib/signals.py`, `lib/presentation.py`, `tests/test_signals.py`, and `tests/test_presentation.py` source in memory without writing package bytecode caches; result: `compiled=5`
- ran `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s "<repo-root>/plugin/memory-context-intelligence/tests" -p "test_*.py"` and passed 8 focused tests
- ran `bash "<repo-root>/plugin/memory-context-intelligence/bin/memory-context-intelligence" --help` and confirmed `present` and `choose` are listed while external research, candidate packet building, `/additional/` emission, main RULES mutation, install, and publication remain unperformed
- ran a controlled `signals -> present -> choose` sample using temporary JSON files and confirmed `present` rendered one topic in `auto`, `native-first`, `bilingual`, and `fixed` modes with carry-forward blocked before selection, and `choose` selected `topic-001` with selected carry-forward true while research, packet building, `/additional/`, and main RULES mutation flags stayed false
- checked that `<user-runtime-rules>/additional` was not present in the checked local environment during phase 010 verification

## Summary

This version closes phase 010 by making the first user-facing topic review layer real. The runtime can now render phase-009 topic candidates as a readable list, preserve canonical technical identifiers, and record one selected topic before later carry-forward. External research enrichment, native-agent orchestration, candidate packet building, `/additional/` emission, install/publication, runtime usable release, and main RULES promotion remain gated by later phases.
